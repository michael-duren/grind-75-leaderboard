# Netlify DB (Neon) connection string. On Netlify this is injected automatically
# once you provision the database; locally, copy this file to `.env` and paste
# the connection string from the Netlify dashboard (or `netlify env:get`).
NETLIFY_DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
